# meminfoApp

This Linux program reads data from the /proc/meminfo file, removes all ":" characters from the text
and replaces zero values with dashes.

The results sorted by the memory size are written to a new file.
